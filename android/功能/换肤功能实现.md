layoutInflater.createViewFromTag

```java
            View view;
            if (mFactory2 != null) {
                view = mFactory2.onCreateView(parent, name, context, attrs);
            } else if (mFactory != null) {
                view = mFactory.onCreateView(name, context, attrs);
            } else {
                view = null;
            }
```


这样我们就可以通过自己实现 mFactory2 来实现换肤。



系统的 LayoutIn